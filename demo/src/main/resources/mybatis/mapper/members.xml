<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.example.demo.dao.getMemberDao">

	<select id="selectMember" resultType="int">
		SELECT count(username) usercnt
		FROM
			member
		where username = '${userName}'
		and   password = '${password}'
	</select>
	
	<select id="selectGetMember" resultType="int">
		SELECT count(username) usercnt
		FROM
			member
		where username = '${userName}'
		and   password = '${password}'
	</select>

	<select id="selectPostMember" resultType="int">
		SELECT count(username) usercnt
		FROM
			member
		where username = '${userName}'
		and   password = '${password}'
	</select>
</mapper>
